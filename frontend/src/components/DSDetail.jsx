import React from "react";
import { Link, useNavigate, useParams } from "react-router-dom";

function DSDetail() {
  let { id, ds } = useParams();
  return <div>DSDetail</div>;
}

export default DSDetail;
